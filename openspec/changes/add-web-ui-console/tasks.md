## 1. 前端工程初始化

- [ ] 1.1 创建顶层 `web/` 前端工程，包含包元数据、TypeScript 配置、源码结构和构建/测试脚本。
- [ ] 1.2 配置本地前端开发服务器，将 `/api/**` 代理到 8080 端口的 Spring Boot 后端。
- [ ] 1.3 增加应用外壳，覆盖角色感知导航、响应式布局和第一屏工作台路由。
- [ ] 1.4 增加共享样式基础、图标使用、加载状态、空状态和可复用表格/表单/对话框组件。

## 2. API Client 和身份

- [ ] 2.1 实现聊天、会话、消息、console、知识库、工具、编排和发布端点的 typed API client。
- [ ] 2.2 实现共享后端错误解析器，覆盖结构化错误响应和非标准授权失败。
- [ ] 2.3 实现本地开发身份上下文，覆盖 tenant、user、roles 和 departments。
- [ ] 2.4 确保特权调用以后端授权为准，并在被拒绝时展示访问被拒绝状态。

## 3. 聊天控制台

- [ ] 3.1 实现会话列表加载、当前会话选择、会话删除和消息历史加载。
- [ ] 3.2 实现消息输入，覆盖 Agent/session 上下文、RAG 启用、知识检索数量控制，以及发送/取消行为。
- [ ] 3.3 实现 `POST /api/chat/stream` 消费，覆盖 fetch stream 解析、typed event 处理、terminal 状态、取消和失败恢复。
- [ ] 3.4 展示 assistant 回答的引用、无答案状态、运行状态，并在 stream 失败时保留部分响应。
- [ ] 3.5 在聊天工作区展示工具状态和待确认提示。

## 4. 管理视图

- [ ] 4.1 实现 Agent 管理，覆盖 Agent 列表和 prompt/运行配置更新。
- [ ] 4.2 实现工具管理，覆盖工具列表和启用/禁用。
- [ ] 4.3 实现知识管理，覆盖知识源列表、状态元数据、可见范围、版本、撤销和已支持动作。
- [ ] 4.4 实现 Skill 管理，覆盖版本列表以及已支持的 approve、publish 和 disable 生命周期动作。
- [ ] 4.5 为后端 API 暂不支持的管理操作增加禁用态或未来动作状态。

## 5. 运营、审计、发布和编排视图

- [ ] 5.1 实现指标视图，覆盖会话、模型调用、工具调用、RAG 结果、失败、耗时、Token 成本和反馈数量。
- [ ] 5.2 实现按 Agent 和模型提供方过滤的成本报表。
- [ ] 5.3 实现审计搜索过滤器，覆盖用户、会话、资源、动作和时间范围。
- [ ] 5.4 实现发布就绪视图，覆盖 MVP 场景、阶段门禁、回滚动作和验收清单。
- [ ] 5.5 实现编排 trace 视图，并避免触发有副作用的 GET 操作。

## 6. 后端契约缺口

- [ ] 6.1 将前端消息历史调用对齐到已实现的 `GET /api/messages` 端点，并按需更新文档或后端路由。
- [ ] 6.2 决定是否填充 `UserConsoleView` 的 latest citations 和 file uploads，或保持前端从聊天响应中获取引用。
- [ ] 6.3 决定是否新增 console 专用知识删除、Skill rollback 和工具注册/授权端点，或将这些动作展示为暂不支持。
- [ ] 6.4 记录内存态管理和 telemetry 状态的 runtime-only 持久化限制。

## 7. 验证和文档

- [ ] 7.1 增加 API client 错误处理、stream 解析、身份上下文和角色感知导航的单元或组件测试。
- [ ] 7.2 增加浏览器工作流验证，覆盖工作台加载、聊天发送/接收、会话历史、引用或无答案状态，以及取消行为。
- [ ] 7.3 增加浏览器工作流验证，覆盖至少一个管理视图和一个运营或审计视图。
- [ ] 7.4 使用截图验证桌面和移动端响应式布局，并修复文本溢出或元素重叠。
- [ ] 7.5 记录后端和前端本地启动命令、生产托管选项、身份假设和已知后端限制。
- [ ] 7.6 在标记变更完成前运行 OpenSpec 校验以及相关后端/前端测试命令。
