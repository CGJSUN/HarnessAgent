## 1. 范围与迁移准备

- [x] 1.1 盘点现有企业版 API、服务、前端视图和文档中出现的租户、RBAC、审计报表、运营后台和发布门禁概念。
- [x] 1.2 增加个人版 `personal` profile，并定义 owner、agent、session、workspace 的默认上下文映射。
- [x] 1.3 建立 AgentScope Java v2 官方文档覆盖矩阵，记录页面 URL、最后更新时间、capability 映射、实现状态和测试状态。
- [x] 1.4 更新项目 README、`docs/` 和 `web/README.md` 的产品定位，明确个人版目标和企业级非目标。

## 2. 个人 Agent Core

- [x] 2.1 调整聊天请求和运行命令，使个人版默认不要求企业租户或企业角色字段。
- [x] 2.2 保留内部 owner、agent、session 状态 key，确保不同个人 Agent 和会话隔离。
- [x] 2.3 实现个人版非流式聊天 API 返回消息标识、会话标识、内容块和执行摘要。
- [x] 2.4 实现类型化流式事件，覆盖文本增量、模型状态、工具事件、子 Agent 事件、错误和完成。
- [x] 2.5 将消息存储调整为统一 ContentBlock 模型，覆盖文本、文件引用、图片/音视频占位、模型思考和工具结果。
- [x] 2.6 完善 ModelProvider 配置，支持个人 Agent 级 provider、模型、密钥引用、预算和 fallback。
- [x] 2.7 实现 AgentState 会话恢复，覆盖应用重启后的消息历史、上下文和待处理状态。
- [x] 2.8 增加核心测试，覆盖个人上下文、Agent 隔离、非流式聊天、流式事件、ContentBlock、fallback、预算、超时和取消。

## 3. 个人 Workspace Runtime

- [x] 3.1 定义个人工作区目录和元数据结构，覆盖 persona、memory、skills、subagents、plans、sessions 和 artifacts。
- [x] 3.2 实现工作区初始化和路径安全校验，阻止路径穿越和未授权文件访问。
- [x] 3.3 实现工作区文件上传、生成文件保存、引用定位、下载和删除。
- [x] 3.4 增加沙箱执行策略抽象，覆盖本地子进程、Docker 或远端沙箱适配点。
- [x] 3.5 为代码、Shell、SQL 或不可信工具执行接入沙箱策略和用户确认。
- [x] 3.6 实现工作区和沙箱快照引用，支持长任务恢复。
- [x] 3.7 实现上下文压缩，保留目标、状态、关键发现、决策、文件引用和下一步。
- [x] 3.8 实现 Plan Mode，只读生成计划并持久化到 `workspace/plans/`。
- [x] 3.9 实现 channel 分层，区分用户可见消息、工具事件、计划更新、系统提醒和诊断信息。
- [x] 3.10 增加 workspace 测试，覆盖初始化、路径拒绝、快照恢复、压缩、计划模式和 channel 展示。

## 4. 个人 Memory 与 RAG

- [x] 4.1 实现分层个人记忆模型，覆盖会话上下文、Agent 记忆文件和事实流水账。
- [x] 4.2 实现显式记忆读写工具，支持写入确认、拒绝、删除和可撤销记录。
- [x] 4.3 实现个人知识源接入，支持本地文件、目录或 URL 元数据。
- [x] 4.4 实现个人知识解析、切片、索引、版本和索引状态追踪。
- [x] 4.5 实现个人 RAG 检索，组合个人知识源和记忆，并返回引用来源。
- [x] 4.6 实现无答案策略，证据不足时拒绝编造引用。
- [x] 4.7 抽象 memory/RAG provider，保留 Bailian、Mem0、ReMe、Dify、Haystack、RAGFlow 和本地 provider 适配点。
- [x] 4.8 实现个人数据删除和导出，覆盖记忆、知识源、索引元数据和引用记录。
- [x] 4.9 增加 memory/RAG 测试，覆盖记忆写入、知识索引、引用来源、无答案、provider 切换、删除和导出。

## 5. 个人 Tooling 与 HITL

- [x] 5.1 实现个人工具注册表，记录 schema、风险等级、权限策略、来源和输出类型。
- [x] 5.2 实现工具参数校验，覆盖 schema、白名单、工作区路径和上下文策略。
- [x] 5.3 实现工具权限三态决策：允许、需要确认、拒绝。
- [x] 5.4 实现 Human-in-the-loop 暂停点，支持用户确认、拒绝或修改参数后恢复。
- [x] 5.5 将工具结果保存为结构化事件和 ContentBlock，并提供摘要与原始引用。
- [x] 5.6 为有副作用工具增加幂等标识、冲突处理和脱敏最小审计。
- [x] 5.7 接入 MCP 或协议适配器工具来源，并复用个人工具治理链路。
- [x] 5.8 增加工具测试，覆盖参数拒绝、只读允许、写操作确认、HITL 恢复、结构化结果、幂等和 MCP 来源。

## 6. 个人多 Agent 编排

- [ ] 6.1 实现工作区内子 Agent 规格，覆盖名称、用途、输入输出、工具、技能和上下文边界。
- [ ] 6.2 实现 Supervisor 路由，根据任务意图、能力描述、上下文边界和授权选择子 Agent。
- [ ] 6.3 实现同步和后台子 Agent 委派，并通过事件或系统提醒返回后台终态。
- [ ] 6.4 实现 Agent-as-Tool 调用路径，保留工具契约、权限决策和 trace 引用。
- [ ] 6.5 实现 handoff 上下文边界，控制消息、文件、记忆和工具结果共享。
- [ ] 6.6 实现多 Agent trace，覆盖 Supervisor 决策、候选 Agent、选中 Agent、handoff、工具调用和结果组装。
- [ ] 6.7 实现编排失败处理，支持澄清、重试、降级到主 Agent 或停止执行。
- [ ] 6.8 增加多 Agent 测试，覆盖子 Agent 规格、路由、后台委派、Agent-as-Tool、上下文限制、trace 和失败降级。

## 7. 个人 Skill 集成

- [ ] 7.1 实现本地技能仓库扫描，并预留 Git、MySQL 和 PostgreSQL 仓库适配器。
- [ ] 7.2 定义技能元数据模型，覆盖名称、描述、触发条件、版本、来源、权限、依赖资源和适用 Agent。
- [ ] 7.3 实现技能加载和执行，将技能指令和资源注入 Agent 执行上下文。
- [ ] 7.4 将技能请求的文件、工具、网络、沙箱和记忆权限纳入个人授权决策。
- [ ] 7.5 实现技能启用、禁用、升级、回滚和版本锁定。
- [ ] 7.6 实现技能验证入口，检查元数据、引用文件、依赖工具和基本执行样例。
- [ ] 7.7 增加技能测试，覆盖仓库加载、元数据展示、触发执行、权限拒绝、版本回滚和无效技能拒绝。

## 8. 个人 Agent Workbench

- [ ] 8.1 重构前端导航和信息架构，从企业管理/运维控制台调整为个人 Agent 工作台。
- [ ] 8.2 实现聊天工作台，支持流式文本、类型化事件、文件附件、引用来源、工具确认和子 Agent 状态。
- [ ] 8.3 实现任务和计划视图，展示计划文件、当前步骤、阻塞点和完成状态。
- [ ] 8.4 实现工作区文件视图，支持浏览、预览、上传、下载、删除和消息引用跳转。
- [ ] 8.5 实现知识和记忆管理，支持查看、添加、删除、导出和重建。
- [ ] 8.6 实现工具和技能管理，支持启用、禁用、配置、授权和测试。
- [ ] 8.7 实现 Agent 配置视图，覆盖名称、system prompt、模型 provider、预算、工作区策略、记忆策略、工具和技能。
- [ ] 8.8 实现 trace 和诊断视图，展示模型、工具、RAG、子 Agent、错误和可操作诊断。
- [ ] 8.9 增加前端单元和浏览器测试，覆盖桌面和移动视口中的聊天、确认、文件、记忆、工具、技能和 trace。

## 9. 覆盖校验与发布准备

- [ ] 9.1 为 AgentScope Java v2 覆盖矩阵增加自动或半自动校验脚本。
- [ ] 9.2 对所有 OpenSpec artifacts 运行校验，修复 requirement、scenario 和 task 格式问题。
- [ ] 9.3 运行后端相关测试，覆盖个人上下文、状态恢复、工具 HITL、RAG、技能和多 Agent 编排。
- [ ] 9.4 运行前端 `npm run test:unit`、`npm run test:browser` 和 `npm run build`。
- [ ] 9.5 更新发布说明和回滚文档，说明如何禁用个人版新入口、工具、技能、RAG provider 和协议适配器。
- [ ] 9.6 完成端到端验收，覆盖个人聊天、工作区文件、记忆/RAG、工具确认、子 Agent、技能和工作台诊断。
