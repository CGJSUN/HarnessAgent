## 1. 企业 Agent 核心 MVP

- [x] 1.1 创建 Spring Boot + Maven + JDK 17 项目结构。
- [x] 1.2 引入 AgentScope Java v2 HarnessAgent 依赖。
- [x] 1.3 增加模型配置加载和 ModelProvider 抽象。
- [x] 1.4 创建 HarnessAgent builder 配置，覆盖 name、system prompt、model、workspace 和 compaction。
- [x] 1.5 实现基于 tenantId、userId、agentId、sessionId 的 RuntimeContext key 派生规则。
- [x] 1.6 实现 `POST /api/chat` 非流式 Agent 调用。
- [x] 1.7 实现会话列表、消息列表和删除会话 API。
- [x] 1.8 实现流式聊天事件，覆盖文本增量、状态、错误和完成事件。
- [x] 1.9 增加核心测试，覆盖聊天执行、会话隔离、消息历史和流式完成。

## 2. 知识库 RAG

- [x] 2.1 定义知识源元数据模型，覆盖租户、归属人、版本、可见范围和更新策略。
- [x] 2.2 实现文档上传或同步入口。
- [x] 2.3 实现解析、切片、索引和知识源版本追踪。
- [x] 2.4 实现关键词检索、向量检索和重排序的混合检索配置。
- [x] 2.5 在检索过程中强制执行租户、部门、角色、用户和知识源特定权限过滤。
- [x] 2.6 为知识库支持的回答返回引用来源元数据。
- [x] 2.7 实现知识源删除、撤销和版本失效行为。
- [x] 2.8 在可访问证据不足时实现无答案行为。
- [x] 2.9 增加 RAG 测试，覆盖索引、权限过滤、引用来源、失效、无答案行为和检索指标。

## 3. 受治理的工具调用

- [x] 3.1 实现企业工具注册表，覆盖 schema、归属人、风险等级、权限策略和审计策略。
- [x] 3.2 为首批企业集成增加只读工具执行路径。
- [x] 3.3 为写入、发送、提交、执行和创建类操作增加高风险工具分级。
- [x] 3.4 在工具执行前强制检查用户、租户、Agent 和工具权限。
- [x] 3.5 根据结构化 schema 和白名单校验工具参数。
- [x] 3.6 为高风险工具增加用户确认或审核人审批流程。
- [x] 3.7 为会修改外部系统的工具增加幂等处理。
- [x] 3.8 持久化脱敏后的工具审计记录。
- [x] 3.9 在同一治理控制下接入 MCP-backed 工具来源。
- [x] 3.10 增加工具治理测试，覆盖权限拒绝、参数拒绝、确认、幂等、审计记录和 MCP 治理。

## 4. 生产运行时

- [x] 4.1 增加 RedisDistributedStore 和/或 MysqlDistributedStore 配置。
- [x] 4.2 阻止多副本生产 profile 使用本地 JsonFileAgentStateStore。
- [x] 4.3 实现带隔离范围的租户感知状态 key 策略。
- [x] 4.4 为非代码类 Agent 增加 RemoteFilesystemSpec 配置。
- [x] 4.5 为代码、Shell、SQL 或不可信执行类 Agent 增加沙箱工作区配置。
- [x] 4.6 配置 OSS、S3、MinIO 或 JDBC-backed 沙箱快照存储。
- [x] 4.7 增加 OpenTelemetry trace 和 metrics，覆盖 API、Agent、模型、RAG、工具、Token、耗时、异常和反馈事件。
- [x] 4.8 按租户、用户、Agent 和模型提供方实现限流和预算。
- [x] 4.9 为可重试的模型提供方失败实现模型 fallback 策略。
- [x] 4.10 为长时间流式响应、工具调用和沙箱执行增加超时与取消处理。
- [x] 4.11 增加生产运行时测试，覆盖分布式状态、副本切换、限流、fallback、trace 和超时。

## 5. 安全治理

- [x] 5.1 接入选定的企业身份提供方。
- [x] 5.2 为 Agent、工具、知识源、管理操作和运营数据实现 RBAC。
- [x] 5.3 在 RAG、工具、记忆、文件和审计视图中强制数据权限一致性。
- [x] 5.4 增加 Prompt Injection 和不安全工具调用控制。
- [x] 5.5 将模型 key、企业凭证和工具 token 迁移到已批准的密钥存储。
- [x] 5.6 为日志、trace、审计记录和诊断信息增加敏感字段脱敏。
- [x] 5.7 增加高风险操作审计留存和搜索权限。
- [x] 5.8 实现 Skill 审核、版本、发布、禁用和回滚控制。
- [x] 5.9 增加安全测试，覆盖身份、RBAC、数据泄漏、Prompt Injection、密钥处理、脱敏和 Skill 治理。

## 6. 管理和运营控制台

- [x] 6.1 实现用户聊天控制台，覆盖流式输出、历史、引用来源、启用时的文件上传、工具状态和确认提示。
- [x] 6.2 实现 Agent 管理，覆盖 Prompt、模型选择、工作区 profile、压缩策略和已启用能力。
- [x] 6.3 实现工具管理，覆盖注册、启用、禁用、分类和权限。
- [x] 6.4 实现知识库管理，覆盖知识源版本、权限、索引状态和删除。
- [x] 6.5 实现 Skill 管理，覆盖仓库、版本、审批状态、禁用和回滚。
- [x] 6.6 实现运营指标，覆盖会话、模型调用、工具调用、RAG 结果、失败、耗时、Token 成本和反馈。
- [x] 6.7 实现按租户、Agent 和模型提供方聚合的成本和用量报表。
- [x] 6.8 实现带权限控制的审计搜索，覆盖会话、配置变更、工具调用、审批和人工干预。
- [x] 6.9 增加控制台测试，覆盖权限访问、配置审计、指标过滤、成本报表和审计搜索。

## 7. 多 Agent 编排

- [x] 7.1 实现专家 Agent 注册表，覆盖用途、契约、权限、工具、知识访问和归属人元数据。
- [x] 7.2 实现按任务意图、权限范围和专家 Agent 能力进行的 Supervisor 路由。
- [x] 7.3 将已批准的子 Agent 作为工具暴露给父 Agent。
- [x] 7.4 为复杂任务实现 planning、pipeline execution、routing 和 handoff 记录。
- [x] 7.5 对记忆、文件、工具输出和检索知识执行跨 Agent 上下文边界。
- [x] 7.6 增加编排 trace，覆盖 Supervisor 决策、子 Agent 调用、工具调用、handoffs、失败和最终回答组装。
- [x] 7.7 在编排置信度低或策略阻止操作时增加人工干预或升级机制。
- [x] 7.8 增加编排测试，覆盖路由、Agent-as-Tool、上下文共享限制、handoffs、trace 和升级。

## 8. 发布和验证

- [x] 8.1 选择首个业务场景，并记录 MVP 验收标准。
- [x] 8.2 对 proposal、design、spec 和 task artifacts 运行 OpenSpec 校验。
- [x] 8.3 为 MVP、RAG、工具调用、生产运行时、安全治理、控制台和多 Agent rollout 创建阶段门禁。
- [x] 8.4 增加部署回滚流程，覆盖禁用 Agent、工具、RAG 回答、模型提供方和 Skill 版本。
- [x] 8.5 运行端到端验收测试，覆盖租户隔离、权限过滤、高风险确认、审计可追溯和运营可观测性。
